spring:

  datasource:
    url: jdbc:postgresql://localhost:5434/${FIELDSYNC_DB_NAME:fieldsync_dev}
    username: ${FIELDSYNC_DB_USER:fieldsync_app}
    password: ${FIELDSYNC_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false

    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations:
      - classpath:db/migration

server:
  port: 8081